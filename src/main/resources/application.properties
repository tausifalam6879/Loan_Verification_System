spring.application.name=VerificationSystem

# Default local/demo database. This keeps the app runnable even when MySQL credentials are not configured.
spring.datasource.url=jdbc:h2:mem:verification_system_local;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate ORM Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.open-in-view=false

spring.h2.console.enabled=${H2_CONSOLE_ENABLED:true}
spring.h2.console.path=/h2-console

# Server Port
server.port=${PORT:8081}

# JWT Configuration
jwt.secret=${JWT_SECRET:MySuperSecretKeyForLoanVerificationSystem2026JwtToken}

# Comma-separated origins/patterns. Production adds the GitHub Pages origin through an environment variable.
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://192.168.*.*:3000,http://192.168.*.*:3001}

# OTP/email settings. Local default uses console fallback OTP unless SMTP/Twilio is configured.
app.otp.enabled=${APP_OTP_ENABLED:true}
app.otp.console-fallback.enabled=${APP_OTP_CONSOLE_FALLBACK_ENABLED:true}
app.mail.enabled=${APP_MAIL_ENABLED:false}
app.mail.provider=${APP_MAIL_PROVIDER:smtp}
app.mail.from=${APP_MAIL_FROM:${SMTP_USERNAME:}}
app.mail.resend-api-key=${RESEND_API_KEY:}
app.mail.resend-base-url=${APP_MAIL_RESEND_BASE_URL:https://api.resend.com}
app.mail.gmail-client-id=${GMAIL_CLIENT_ID:}
app.mail.gmail-client-secret=${GMAIL_CLIENT_SECRET:}
app.mail.gmail-refresh-token=${GMAIL_REFRESH_TOKEN:}
app.mail.google-oauth-base-url=${APP_MAIL_GOOGLE_OAUTH_BASE_URL:https://oauth2.googleapis.com}
app.mail.gmail-api-base-url=${APP_MAIL_GMAIL_API_BASE_URL:https://gmail.googleapis.com}
app.sms.enabled=${APP_SMS_ENABLED:false}
app.whatsapp.enabled=${APP_WHATSAPP_ENABLED:false}
app.sms.default-country-code=${APP_SMS_DEFAULT_COUNTRY_CODE:+91}
app.otp.provider-connect-timeout-ms=${APP_OTP_PROVIDER_CONNECT_TIMEOUT_MS:5000}
app.otp.provider-read-timeout-ms=${APP_OTP_PROVIDER_READ_TIMEOUT_MS:10000}

spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${SMTP_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:10000}

twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.sms-from=${TWILIO_SMS_FROM:}
twilio.whatsapp-from=${TWILIO_WHATSAPP_FROM:}
twilio.api-base-url=${TWILIO_API_BASE_URL:https://api.twilio.com}

# Optional Python AI/Data Science service. Start ai-fraud-service on port 8000.
app.ai.service-url=${AI_SERVICE_URL:http://localhost:8000}
